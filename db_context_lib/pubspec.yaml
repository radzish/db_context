name: db_context_lib
description: A starting point for Dart libraries or applications.

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  logging: ^1.0.2
  resource_pool:
    git:
      url: https://github.com/radzish/resource_pool.git

dev_dependencies:
  pedantic: ^1.11.1
  test: ^1.18.2
